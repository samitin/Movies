<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
<FrameLayout
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".view.MainActivity"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintBottom_toTopOf="@id/button_navigation_conteiner"/>
    <LinearLayout
        android:id="@+id/button_navigation_conteiner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_navigation_home"
            style="@style/buttonNavigationStyle"
            android:text="@string/button_navigation_text_home"
            app:icon="@drawable/home"/>
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_navigation_favorite"
            android:text="@string/button_navigation_text_favorite"
            style="@style/buttonNavigationStyle"
            app:icon="@drawable/favorite"/>
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_navigation_reting"
            android:text="@string/button_navigation_text_rating"
            style="@style/buttonNavigationStyle"
            app:icon="@drawable/rating"/>

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>